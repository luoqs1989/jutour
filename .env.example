# Copy this file to .env.local for local development (it is git-ignored).
# Without this key the site automatically falls back to the bundled demo
# attraction data and clearly labels it as such — see src/services/visitSeoulApi.ts.
VITE_VISITSEOUL_API_KEY=
